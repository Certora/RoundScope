# Utils

This directory contains utility scripts used for internal development and batch testing.

`batch_roundabout.py` is intended for batch-running `RoundAbout` over multiple configurations during internal testing. It is not currently a supported end-user workflow, and its interface and behavior may change without notice.
