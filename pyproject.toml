[project]
name = "wala-solidity"
version = "0.0.1"
requires-python = ">=3.8"
dependencies = [
    "json5",
    "packaging",
    "pygments",
    "tqdm",
]

[tool.pytest.ini_options]
testpaths = ["tests", "viewer/tests", "utils/tests"]
